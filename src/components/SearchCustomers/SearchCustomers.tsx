import { observer } from "mobx-react-lite"
import { Form } from "react-bootstrap"


const SearchCustomers:React.FC = observer(()=>{

    return <div>
        <Form.Control/>
    </div>
})

export default SearchCustomers